// Test setup file for common configuration
import 'reflect-metadata';

// Global test configuration
beforeAll(async () => {
  // Setup global test configuration here
});

afterAll(async () => {
  // Cleanup global test configuration here
});
